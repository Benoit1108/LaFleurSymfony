{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1 style="margin-top:20px">Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" style="margin-right: 10px" href="{{ path('categorie_index') }}">Retour</a>

    <a class="btn btn-warning" href="{{ path('categorie_edit', {'id': categorie.id}) }}">Editer</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
