<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
	<div class="container">
		{% if app.user %}
			<a class="navbar-brand" href="{{path('accueil_news')}}">LA FLEUR ... Une passion à fleur de peau |
				{{ app.user.prenom }}
				{{ app.user.nom }}
				{% if is_granted('ROLE_ADMIN') %}
					(ADMIN)
				{% endif %}
			</a>
		{% else %}
			<a class="navbar-brand" href="{{path('accueil_news')}}">LA FLEUR ... Une passion à fleur de peau</a>
		{% endif %}

		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarResponsive">
			<ul class="navbar-nav ml-auto">

				<li class="nav-item">
					<div class="btn-group">
						{% if app.user %}
							{% if is_granted('ROLE_ADMIN')%}
								<button type="button" class="btn btn-sm btn-warning dropdown-toggle" data-toggle="dropdown" style="height:41px">
									Gestion
									<span class="caret"></span>
								</button>
								<ul class="dropdown-menu" role="menu">
									<li>
										<a href="{{path('news_index')}}">News</a>
									</li>
									<li>
										<a href="{{path('utilisateur_index')}}">Utilisateurs</a>
									</li>
									<li>
										<a href="{{path('fleur_index')}}">Fleurs</a>
									</li>
									<li>
										<a href="{{path('categorie_index')}}">Catégorie</a>
									</li>
								</ul>
							{% endif %}
						{% endif %}
					</div>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{path('accueil_news')}} ">Accueil
						<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('fleur')}}">Catalogue</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Contact</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="#">Profil</a>
					</li>
				{% endif %}

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link btn btn-danger" style="margin-right:10px" href="{{path('app_logout')}}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link btn btn-info" style="margin-right:10px" href="{{path('app_login')}}">Connexion</a>
					</li>
				{% endif %}
				<li class="nav-item">
					<a href="{{path('panier_index')}}" type='button' class='btn btn-primary' style="width:70px; height:41px">
						<i class='fas fa-shopping-cart'>
							{{ render(controller('App\\Controller\\PanierController::nbProdPanier'))}}

						</i>
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
