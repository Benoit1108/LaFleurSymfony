<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
	<div class="container">
		{% if app.user %}
				<a class="navbar-brand" href="{{path('accueil_news')}}">LA FLEUR ... Une passion à fleur de peau | {{ app.user.prenom }} {{ app.user.nom }}
					{% if is_granted('ROLE_ADMIN') %}
						(ADMIN)
					{% endif %}
				</a>
		{% else %}
			<a class="navbar-brand" href="{{path('accueil_news')}}">LA FLEUR ... Une passion à fleur de peau</a>
		{% endif %}

		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarResponsive">
			<ul class="navbar-nav ml-auto">

				<li class="nav-item">
					<div class="btn-group">
						{% if app.user %}
							{% if is_granted('ROLE_ADMIN')%}
							<button type="button" class="btn btn-sm btn-warning dropdown-toggle" data-toggle="dropdown">
								Gestion
								<span class="caret"></span>
							</button>
							<ul class="dropdown-menu" role="menu">
								<li>
									<a href="">News</a>
								</li>
								<li>
									<a href="">Utilisateurs</a>
								</li>
							</ul>
							{% endif %}
						{% endif %}	
					</div>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{path('accueil_news')}} ">Accueil
						<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('fleur')}}">Catalogue</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Contact</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="#">Profil</a>
					</li>
				{% endif %}

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">Connexion</a>
					</li>
				{% endif %}
				<li class="nav-item">
					<a href="" type='button' class='btn btn-primary'>
						<i class='fas fa-shopping-cart'>&nbsp;</i>
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
